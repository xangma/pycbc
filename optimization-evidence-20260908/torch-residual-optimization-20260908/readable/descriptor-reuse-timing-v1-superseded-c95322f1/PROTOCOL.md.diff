--- /Users/xangma/repos/pycbc/artifacts/torch-residual-optimization-20260908/descriptor-reuse-v1/PROTOCOL.md
+++ PROTOCOL.md
@@ -1,21 +1,44 @@
-# Bounded original-function descriptor reuse qualification
+# Descriptor reuse timing v1
 
-This diagnostic keeps the qualified production source `ecd5d08231d8ce0a938bc31cfde27c9a5d6f901f` unchanged. The original function API creates MKL descriptors repeatedly during shared CPU setup in both standard and Torch CUDA executables. The prior instrumented CUDA diagnostic measured about 1.52 s of repeated creation per run; this motivates reuse but does not establish recoverable time or a speedup. This package first gates actual MKL reuse and then runs four instrumented science qualifications. No elapsed duration here supports a performance claim.
+## Scope and prior evidence
 
-Only three single-precision, out-of-place, one-thread CPU configurations are eligible: real-to-complex transforms with real lengths 16,384 and 16,777,216, and complex-to-real with real length 16,777,216. The full immutable key includes direction, exact input/output lengths and canonical dtypes, precision/kinds, maximum size, placement, actual scheme class, four device attributes, thread count, process and thread object. At most three descriptors are retained. Unsupported configurations and a fourth distinct key call the original function. The cache owns no array, exported view or buffer; native compute receives each call's current pointers. It is main-thread/PID bound, rejects fork/thread/reentry reuse, and explicitly releases all owned handles before a completed receipt.
+Artifact-only fast Python wrapper on the qualified clean ecd5d08231d8ce0a938bc31cfde27c9a5d6f901f source. No production edits, C/CUDA/native symbol replacement, integration or publication. The frozen instrumented qualification passed native and both-scheme science gates, with independent replay ca074bc81beb3eb807802bde1b1327e6545b2fb05c26b038b6f7d5b56731d71a. Its durations are not performance evidence.
 
-`descriptor_reuse.py` hashes the original MKL source and proves invertible AST changes: the original function's free expression becomes a private release hook, and the original creator gets one ownership observer immediately after allocation. All successful-path original setup operations, CCS layout, signature assignments and status-check sequence remain. Cached hits reuse that descriptor; an uncached control uses identical import/row bookkeeping and frees each successful call. Native symbols remain original and castable, and class APIs remain original. Original functions themselves assign ctypes argument types, so the contract is original-derived signature transitions and call order, not unchanged argument types across a call. No blanket reset is applied. The promoted Torch IFFT uses its own two-argument callable and must coexist with function and class plans.
+## Exact helper and fairness
 
-Creation with a nonzero status, null handle or sentinel 1 deliberately fails closed before unsafe original setup; ambiguous handles are not claimed or freed. A confirmed allocation is freed once on a later Python/setup error. Native compute errors retain the original error as primary after release. A failed free poisons the cache, marks ownership uncertain and is never retried. Cleanup and invariant errors remain in receipts without replacing the primary workload error. Close and restoration are attempted independently. Original commit return values are ignored by original code; `create_successes` means the original creator returned after commit invocation, not an independently checked successful commit. No new native status query or unrelated production fix is introduced.
+`descriptor_reuse.py` subclasses byte-identical frozen `descriptor_reuse_diagnostic.py`. Unchanged inherited code owns the inverse-verified original MKL AST clones, PID/current-thread ownership, native identities, free lifecycle, install/close/restore/finish. The fast overrides remove per-call rows, pointer histories, public-key formatting, signature recording and per-call native-invariant sweeps. Native identity checks remain at construction, install and final boundaries. Every call retains owner, complete key, reentry, capacity and eligible/fallback checks. No per-call clock, profiler, stack capture or hit counter is introduced. Only owned-descriptor lifecycle/error records remain. Successful cached execution owns at most three descriptors.
 
-Local tests execute the actual pinned AST with castable native mocks, checking miss/hit/new pointers, exact baseline order/signatures, capacity/full-key changes, fallback, class coexistence, ownership/lifetime, setup/compute/free faults, primary-error preservation, actual fork/thread/reentry protection and cleanup. Harness tests execute the actual runtime checker against all four current run paths and both archived reference paths, reject wrong paths/hashes/versions/thread/environment controls, reject inconsistent lifecycle receipts, and execute the actual wrapper with workload and cleanup failures. They provide no real numerical or timing evidence. The peer additionally tests the real-native runner's actual final receipt body with stub cleanup states.
+Full key: direction, exact input/output lengths and canonical dtypes, precision/kind tokens, maximum length, actual placement, concrete scheme class, device/device_num/device_spec/torch_device values, scheme threads, PID and owning thread object. Eligibility remains single-thread CPU-scheme out-of-place float32 real-to-complex64 16384->8193 and 16777216->8388609, and the inverse large pair. Unsupported or fourth-distinct configurations invoke exact original functions. No arrays are retained or exported.
 
-The native gate uses nine real transforms: three fresh live input/output pairs for each eligible configuration, comparing complete output bytes against the original and preserving complete input bytes. All 18 input/output pointers must be distinct. A live original class FFT and a live promoted in-place complex128-workspace Torch CPU IFFT execute exactly after every transform and after cache close. The latter retains its two-argument ABI and original complex64 public source. Three creates, six hits, and exactly three successful explicit frees are required, then both coexisting plans are explicitly released. Core 8, Torch intra/inter-op 1, all native pools 1 and three actual dladdr/hash library identities are checked.
+Original creation/set/commit/compute argument signatures and order remain unchanged. Original ignored commit return values remain ignored: creates means original creator returned after commit invocation. Failed/nonzero/null/sentinel/ambiguous allocations deliberately fail closed before setup and do not claim ownership. Confirmed later failures free once; uncertain frees poison without retry. Primary workload errors survive cleanup and boundary errors. Removing hot invariant sweeps deliberately delays detection of native-symbol tampering until a boundary; no native monkey patching or concurrent callers are permitted in this experiment.
 
-Only after this native worker exits successfully does the controller run CUDA uncached, CUDA cached, standard uncached and standard cached serially. Each uses the frozen 384-template compressed bank, 512 s segments, 112/16 s pads, five segments, 1,904 valid detector seconds and 1,920 inverse FFTs. Source, all native/Python/input/helper/reference pins, sanitized environment, affinity and numerical thread pools remain frozen. Torch threads are observed as 1 for CUDA; the unchanged standard runtime checker records null because that executable does not use Torch. Both families retain their original qualified source path. Exact predeclared run locations dispatch to separate verifier module contexts with the proper old or current helper root. Unknown locations fail; no path contract is relaxed.
+Both baseline and candidate import and construct the identical Fast cache at the same point in `run_reuse.py`, including early MKL import and AST construction. Baseline (`--reuse no`) never installs: it calls exact original FFT functions and closes the empty object, checking original-function/native identities at exit. Candidate (`--reuse yes`) installs and finally closes/restores. Each run receipt binds fast helper, diagnostic parent, runner and source hashes. Full child wall includes interpreter startup, imports/cache setup, workload, final frees/restoration, receipts/runtime checks and process exit. Internal inspiral/setup telemetry is secondary.
 
-All four runs require actual persisted PSD content, conditioned-strain, bank and segment equality to their own qualified reference, default trigger comparison, and exact bytes for the 18 H1 science datasets (four timing telemetry datasets remain excluded under the existing contract). Each cached run is also compared against its uncached control. Final CUDA cached triggers must pass the default comparison against standard cached. CUDA requires exactly 1,003 eligible calls: 1,003 creates/frees uncached, or three creates/frees and 1,000 hits cached. Standard call totals are observed, not assumed; full-key group populations must match its own uncached run. Every eligible native status is zero and all owned descriptors must be freed once. Unsupported original calls remain recorded as fallbacks.
+## Gates before measured children
 
-The sole remote owner stages this new directory on `len` at `/home/xangma/pycbc-torch-residual-optimization-20260908/descriptor-reuse-v1`, reviews the frozen peer report, and runs `/home/xangma/pycbc-torch-split-20260905/venv/bin/python manage.py launch` there. The detached controller command is that interpreter followed by `reuse_controller.py`; log `controller.log`, launch identity `launch.json`, worker commands/PID/PGID/start ticks/logs and state `reuse-status.json`. Expect a few minutes; first check within one minute using `manage.py status`. Stop with `kill -TERM -- -<recorded PGID>`. Every worker inherits the existing exclusive lock's exact open file description. Timeout is 1,800 s per worker and terminates the controller group. The independent `manage.py close` requires the whole group absent and lock reacquisition, rechecks frozen pins and seals success or failure with an archive/manifest. Failed attempts are preserved; no overwrite/retry in this directory.
+One controller holds the exclusive inherited benchmark lock throughout. Before any timing it runs a fresh real-MKL gate, then four fresh science qualifications in cell order A,B,C,D. Native gate checks nine cases, three simultaneously retained allocation pairs per eligible configuration, 18 distinct pointers, full output bytes equal to original MKL and preserved input. It externally observes three misses/six hits and coexisting class FFT and promoted complex128-workspace Torch in-place two-argument MKL IFFT plan behavior after every call and cache closure. External hit observations exist only in this gate. Both plans are explicitly released.
 
-After owner and peer review of the completed raw results, a separate balanced unprofiled executable experiment may be prepared for both standard and CUDA with/without common setup reuse, three fresh repetitions each. That later harness must be reviewed separately. No Torch CPU executable comparison or integration/PR/publication is authorized by results in this package alone.
+Each fast baseline/candidate standard/CUDA qualification retains prior 384-template compressed-bank, 512-second segment/112+16-pad geometry, five segments, 1904 valid seconds, 1920 IFFT calls, PSD files/data and conditioned-strain identity gates. Each matches its exact frozen own-scheme reference. Cached versus original passes all 18 H1 scientific datasets byte-for-byte within scheme; standard/CUDA default comparison also passes. Pin changes or any failure stop the controller before timings.
+
+## Prespecified timing schedule and comparisons
+
+A=standard-original (`cpu:1`); B=standard-cached (`cpu:1`); C=cuda-original (`torch:cuda:0`); D=cuda-cached (`torch:cuda:0`). Four fresh serial subprocess repeats, retaining every run:
+
+1. A, B, D, C
+2. B, C, A, D
+3. C, D, B, A
+4. D, A, C, B
+
+Each cell appears once in each position; each within-scheme cached/original pair runs in each direction twice. No warmup runs or adaptive resampling. The native/science gates naturally precede timing, so filesystem/library caches may already be warm; no cold-cache claim.
+
+Timed mode is `timing`, preserving the established metrics validator. Timed children use the same fast runner but invoke the executable directly without qualification/PSD/strain hooks. No descriptor-call instrumentation exists. Runtime checks remain unchanged, including exact helper provenance, source/modules, affinity core 8, native pools one, CUDA torch intra/inter-op one at the relevant boundaries. Standard runtime retains its original no-Torch reporting semantics. All source/input/native/reference pins are checked before and after each worker.
+
+Every timed output passes exact own-qualification H1 comparison and the default comparator against updated standard-cached qualification. Each repeat additionally compares same-scheme cached/original contemporaneously and CUDA-cached against contemporaneous standard-cached. Work inferred from HDF timing metadata must match the qualified templates, segments and valid duration. The fast report confirms three owned descriptors freed once for candidate and an empty baseline object; no timed hit-count claim is made.
+
+Report all raw full-wall samples, per-cell medians/ranges, paired cached-minus-original wall differences and cached/original ratios by repeat for both schemes; compare CUDA-cached against both contemporaneous standard-original and standard-cached, preserving the effect of common setup optimization. Report whole-process speedup as original/cached where used. Internal/setup and throughput telemetry remain secondary. Four samples support a bounded experiment, not a statistical significance claim or general workload guarantee. No Torch-CPU parity claim: this phase does not measure Torch CPU.
+
+## Execution and closure
+
+Host len; cwd `/home/xangma/pycbc-torch-residual-optimization-20260908/descriptor-reuse-timing-v1`. Launch via `/home/xangma/pycbc-torch-split-20260905/venv/bin/python manage.py launch`; command detaches the controller and records actual PID/PGID/start ticks/command/cwd/log in launch.json. Read-only checks use `manage.py status`; expected first check within 30 seconds and every 30-60 seconds while running. Approximate duration 15-20 minutes for native, four qualification and 16 full runs, plus gates. Stop only this recorded group with `kill -TERM -- -<recorded-pgid>` after matching start ticks. Per-worker timeout is 1800 seconds and stops the entire group.
+
+Owner alone controls len and shared lock. After terminal state and zero whole-group members, `manage.py close` independently reacquires the lock, checks final immutable pins, seals audit/results manifest/archive. Acquire archive and independently replay raw outputs and timings locally before a result claim. Failed campaigns retain all artifacts and are never relabeled as successful runs.
