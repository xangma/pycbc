# CPU workspace page-backing protocol v3

Status: requirements frozen before the amended native diagnostic, 2026-09-08. Supersedes v2 only for checked Linux advice-constant compatibility and its associated preflight/contracts. The computation, allocation geometry, coverage gates, numerical matrix, timing schedule and decision rule below remain unchanged. The optimizer owns all `len` operations and the benchmark lock; the profiling peer reviews locally. The failed diagnostic-v1 and its reviewed inputs must be preserved. New execution uses a distinct diagnostic-v2 directory; the completed `torch-residual-optimization-20260908` bundle remains sealed.

## Compatibility amendment and prior failed attempt

The operator reported that diagnostic-v1 stopped on `AttributeError: module mmap has no attribute MADV_NOHUGEPAGE` in the first allocation, before any contract completed or FFT qualification/timing ran. This is an invalid runtime attempt, not evidence about numerical correctness or performance. Seal its logs, input pins, failure status and independent process/lock closure; the peer must inspect the acquired failure evidence. No result from that attempt can support a performance claim.

Read-only operator diagnostics found that this Linux Python build has `mmap.mmap.madvise` but omits both huge-page constant names. The installed x86-64 glibc and asm-generic headers define `MADV_HUGEPAGE=14` and `MADV_NOHUGEPAGE=15`; preserve the header/runtime evidence with the new input set. These values agree with the [Linux v6.8 UAPI definitions](https://raw.githubusercontent.com/torvalds/linux/v6.8/include/uapi/asm-generic/mman-common.h). Python documents that individual advice constants may be absent even where advice is available. [Python mmap constants](https://docs.python.org/3/library/mmap.html#MADV_-Constants).

Retain the existing `mapping.madvise(advice, offset, length)` call. Inside the Linux-only advice path, resolve the two names with `getattr(mmap, 'MADV_HUGEPAGE', 14)` and `getattr(mmap, 'MADV_NOHUGEPAGE', 15)` and assert that the resulting values are exactly 14 and 15 before dispatch. A present but unexpected constant must fail closed. Do not add a libc/syscall wrapper, silently suppress advice errors, issue additional advice, change ranges or bypass the exact PMD-size check. Both calls remain before first touch with the same final arm-specific range. This amendment changes name resolution only.

Add a local dispatch contract using a recording mapping stand-in: verify both missing constants and present/correct constants dispatch the exact expected value, offset and length for N and H; unexpected constants must reject before dispatch. Keep the test's simulated Linux/sysfs state and absence of a real advice syscall explicit. Retain and rerun the existing construction/lifetime/guard/failure contracts under the revised helper; the real Linux gate must exercise the actual method and constants. A successful call alone still cannot establish backing: every original v2 smaps/precision requirement below applies.

Freeze the amended helper, contract count, protocol reference, controller expectations, runtime/header evidence and archive before review/launch in diagnostic-v2. The only schedule change allowed is the added compatibility contract; all original qualification and timing loops remain fixed. This retry addresses a documented failure before measurements, with no timing-selected parameter changes.

## Hypothesis and fixed computation

Changing actual PMD huge-page backing may reduce execution cost of the existing private 32 MiB workspace. Prior attribution measured native execution at 28.861 ms within a 33.409 ms complete call. Those earlier diagnostics reported Torch inter-op 64; this experiment requires intra-op **1** and inter-op **1**, matching executable policy, and must measure its own current baseline.

Use clean source commit `ecd5d08231d8ce0a938bc31cfde27c9a5d6f901f`, `pycbc/fft/torchfft.py` SHA256 `0b9109bbf99a4eb591a4c006b1220ec36c8587e56352352277eb1478cf876c0c`. Retain the exact production `IFFT` and `_MKLCPUDirectIFFTPlan`: 2**21 elements, one private contiguous CPU complex128 in-place workspace, complex64 public input/output, one native thread, unchanged DFTI precision/placement/scaling/settings, unchanged two-argument native ABI, promotion/demotion `Tensor.copy_`, status handling, mutation versions, and eligibility guards. No source, arithmetic, backend, or hot-path replacement.

## Three arms and interpretation

| Arm | Workspace construction | Purpose |
| --- | --- | --- |
| O | Original `torch.empty`, untouched | Current complete-call baseline |
| N | Private anonymous mmap, 2 MiB aligned; range-only `MADV_NOHUGEPAGE` | Matched allocation/alignment control |
| H | Identical mmap geometry and construction; range-only `MADV_HUGEPAGE` | Requested PMD backing |

H versus N tests the backing hypothesis; either versus O includes allocator/alignment effects. Preserve that distinction in all conclusions. Explicitly use `MAP_PRIVATE | MAP_ANONYMOUS` with writable CPU memory: Python's Unix mmap default is shared. Apply advice to the aligned 33,554,432-byte workspace before its first memory access. Record setup and first-touch costs. PMD advice is a request, so actual backing must be measured. [Python mmap](https://docs.python.org/3/library/mmap.html), [Linux THP controls](https://docs.kernel.org/admin-guide/mm/transhuge.html).

No global sysfs or process THP policy changes, `MADV_COLLAPSE`, explicit huge-page reservations, privileged operations, page migration, thread tuning, or additional candidate arms. Keep native pools at 1 and affinity `[8]`. Record actual loaded libraries, versions, thread-pool state, relevant environment, kernel, base-page/PMD sizes, process identity, and the existing THP settings. Host memory availability alone does not establish an idle machine.

## Construction and ownership contracts

1. Use an isolated Torch allocation proxy in a clone of the exact constructor code object; temporarily bind only the plan factory during the target engine construction, before descriptor creation/commit. Verify exactly one intended allocation was intercepted; unrelated arguments or allocations must fail closed or use the untouched allocator without being counted. Restore the original plan-class binding by identity in `finally`, including allocation/advice/guard-protection/frombuffer/create/set/commit/finalizer failures. The original class, constructor code object and global `torch.empty` must remain unchanged; the temporary factory is serial-only. Prove unchanged constructor statements outside the allocation seam and identical `execute`/`can_execute` code.
2. Record mapping base/length, aligned offset, live tensor pointer, shape/stride/dtype/device, storage bounds, and advice range/result. The complete native tensor lies within the owned mapping and does not overlap either public tensor. N and H share all construction choices except advice. Reserve exactly `NBYTES + 2*HUGE` bytes with a strictly positive prefix and tail: `pointer = (base // HUGE + 1) * HUGE`. Assert the base, live interval and both margins are base-page aligned, and that both margin lengths are positive. Give the whole reservation `MADV_NOHUGEPAGE`, then the live interval the arm-specific advice. Apply `mprotect(PROT_NONE)` only to both unused margins, identically in N and H, before frombuffer and first touch. This prevents the N live interval merging with identically advised padding. Record both protection ranges/results and verify live `rw-p` plus non-accessible guard coverage from smaps; never read the guards. Do not trim or explicitly close the mapping; retain automatic mmap ownership. Include failure injection for the first and second protection calls. Reject ambiguous attribution rather than silently treating adjacent storage as this workspace. [Linux mprotect](https://www.man7.org/linux/man-pages/man2/mprotect.2.html).
3. Retain the mmap owner through descriptor free and every live tensor/view referencing it. `torch.frombuffer` shares memory and retains its buffer owner; its storage is nonresizable. Preserve public tensor storage/resizability and keep this private storage difference explicit. Automatic reference retention does not justify explicitly closing a mapping while a tensor can still access it. [PyTorch frombuffer](https://docs.pytorch.org/docs/2.14/generated/torch.frombuffer.html).
4. Exercise success, each construction failure, explicit cleanup twice, and object collection. Check one descriptor free in its creating process, cleanup order, release of temporary buffer exports, no mapping/descriptor leak, restored allocation callable, and no possible native execution after the helper closes. Never probe an unmapped pointer to test this.
5. Use fresh public tensors before any NumPy export for guard contracts: replacement storage/objects, changed shape, overlap, autograd, inference/conjugate/negative views, thread count and owner PID. Verify the original eligibility result without dispatching native execution on an ineligible plan. Test successful execute preserves input bytes/version, increments public output once and private workspace twice. On native status failure, preserve public target bytes/version and propagate the error after recording the private mutation. Do not add a new execution guard to improve the benchmark.

## Exact-range page receipts

Read smaps outside all timed regions, after the common first touch/warmup and immediately before and after each timing block. Save the actual mapping header(s) and relevant fields (`Size`, `Rss`, `Anonymous`, `AnonHugePages`, `Swap`, `KernelPageSize`, `MMUPageSize`, `THPeligible` when present, and `VmFlags`), live pointer, requested interval, and timestamp. Repeat pointer/ownership checks at these boundaries.

For N and H require an exact live workspace VMA, full resident anonymous storage, and zero swap. N must have `AnonHugePages=0`; H must have `AnonHugePages=32768 kB`, with the appropriate advice flags. Require these values at both boundaries; partial/absent/changing coverage or an indistinguishable control stops as **inconclusive**, without retries selected by timing. Checking advice flags or `KernelPageSize` alone is insufficient. AnonHugePages reports PMD-mapped anonymous THP; this does not identify all forms of smaller THP. [Linux monitoring](https://docs.kernel.org/admin-guide/mm/transhuge.html#monitoring-usage).

For untouched O save every VMA intersecting its exact workspace interval. If a containing VMA extends outside the workspace, do not assign its total huge bytes to the workspace or split it with advice. Report coverage bounds from overlap and outside bytes, or unknown coverage when it cannot be resolved. Zero AnonHugePages across all intersecting VMAs establishes zero PMD coverage for O. This limitation cannot substitute for the exact N/H contrast.

## Fresh numerical gate

Keep the existing frozen generator and FFTW error budgets from `qualify_runtime.py` SHA256 `e77603961c1f066647065e2e5c86dd2a4676fc891192bab508dc00d926888297`: seeds `(7, 91, 812, 20260906)` × patterns `(dense, banded, impulse)` × scales `(1e-12, 1, 1e12)`, in that order, at 2**21. All 36 tuples must appear once per arm/pass; no seed selection or tolerance floor.

For the same freshly assigned inputs, require both N and H to equal untouched O **bit for bit across the entire native complex128 result and entire final complex64 output**. Compare bytes or integer views, including signed zero; cast-output parity alone is insufficient. Capture native contents after execution and before the next promotion, without changing native execution. Also retain the existing final-output matching-precision MKL parity and both L2/max-absolute errors no greater than the FFTW reference against promoted NumPy truth. Verify input preservation and versions. Every fresh timing worker must qualify its exact retained plans before and after timing. Any numerical mismatch rejects this candidate, even if the old FFTW budget passes. First establish real native construction/lifetime contracts and the full numerical gate under the owner lock, before timing.

## Bounded timing and decision

After both reviewers accept exact helper/harness bytes, freeze their hashes, references, worker count and all loops/order before launch. Accepted fixed schedule: three fresh workers with construction order rotated by worker; within each, all six permutations of O/N/H, with permutation order rotated by worker. Each arm/block uses 3 warmups and 15 complete-call samples on seed 7 dense scale 1. Complete-call timing invokes the unchanged production engine, including both conversions, checks and version/status work. Preserve raw samples; compare block medians and worker medians, not isolated best samples.

Collect native-only attribution in a separate, equally balanced pass. Restore the same complex64 input by the original promotion before every native call, outside that native timer; retain the original two-argument call and status/version handling. Do not repeatedly transform the already transformed in-place output. Keep page reads, output snapshots, hashes and diagnostics outside timers. Report allocation/advice/frombuffer/commit construction, first execution and cleanup separately, as well as complete-call costs; do not claim setup is free or infer executable wall time from native time.

Stop as rejected on contract/numerical failures; inconclusive on missing backing contrast or runtime/provenance failure; no benefit on consistently non-improving complete calls. Only a consistent H complete-call gain versus both N and O across all three fresh workers, supported by the native attribution and all gates, warrants executable qualification. Mixed/noisy results do not trigger open-ended reruns. A new experiment requires an explicitly revised protocol. No production integration, CPU-parity claim, GPU claim or multicore/general-workload claim follows from this microbenchmark.

Retain failure receipts as well as successful samples, controller/worker exit status, lock provenance, exact command/PID/process group/start identity/log/stop command, source state before/after, input/runtime hashes and terminal cleanup audit. Freeze a result manifest for independent local review. Subsequent executable qualification must use the already accepted workload/science checks and include all allocation/first-touch/cleanup costs.
