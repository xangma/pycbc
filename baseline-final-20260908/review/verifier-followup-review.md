# Final bounded verifier follow-up review

No remaining blocker was found within the three-fix scope. All findings in `verifier-review.md` are addressed by the frozen revision below. This clears the verifier follow-up for sealing with the disclosed limitations; it does not establish scientific equivalence or permit an equal-output speedup claim.

Reviewed `verify-results.py` SHA-256 `61040d988b30236cfcf92696e347d4a04e3f3966310881c6460b7d6972ab514b` against preserved `verify-results-v1.py` SHA-256 `b71b189b287329e7eed4c9e7158482176391a811133f7150123cd18723dc211d`. The current verifier hash was identical before and after review/testing and matches the author's frozen hash and final verification receipt. The diff is limited to dependency reconciliation, mandatory timing parity, clock consistency, their focused self-tests, and corresponding report fields. Existing source/hash, schedule, comparator normalization, full-PSD/used-PSD, tolerance and eligibility logic is retained.

1. **Dependency mismatch: addressed by explicit, bounded reconciliation.** The diagnostic and its inspection script have frozen hashes checked before use. The diagnostic binds the unchanged dependency-manifest and completed timing-status hashes, the same interpreter, and an observation after completion. It records two distinct installation roots and reproduces first-match metadata/import version `2.13.0+cu130` versus last-wins dictionary version `2.1.1`. Selected `torch/__init__.py` and `torch/version.py` hashes match each installation's RECORD encoding; the imported post-run paths and selected hashes identify the first installation. All Torch worker version, CUDA-version and hostname receipts must agree with the diagnostic. Missing reconciliation retains the original strict version comparison; altered diagnostic bytes or inconsistent relationships fail. The original manifest is preserved, and the result is explicitly `RECONCILED_WITH_POST_RUN_DIAGNOSTIC`.

2. **Impossible completed timing verdict: resolved.** `compare_timing` now requires every timing comparison against its own qualification to return `pass` before accepting the sample. Both `fail` and `review` raise evidence errors, matching the frozen producer's completion invariant. Permitted cross-arm scientific failures remain visible.

3. **Unchecked timing magnitudes: resolved.** Every process duration is checked against GNU elapsed time, runtime timestamps and enclosing receipt timestamps. Parsing requires one elapsed field and one successful exit status, supports minute/hour formats, and accounts for displayed precision. Runtime containment and positive finite spans are required. The explicit 1 s launch/wait, 5 s runtime-boundary, 60 s enclosing-overhead and 0.05 s wall-clock allowances match the documented producer boundaries. These conservative consistency bounds neither modify samples nor estimate measurement uncertainty; they do not detect every possible small timing error.

I independently ran `--self-test` with the isolated publication Python environment: **47 checks passed**. After the author confirmed final readiness and requested immediate finalization without further broad testing, I inspected the primary's complete `verification.json` rather than repeating that run. It identifies the reviewed verifier hash and reports **evidence PASS, scientific FAIL, equal-output eligibility false**. All **16** timing comparisons pass, and all **20** process clock checks pass. Perf minus GNU elapsed ranges 0.003176–0.059548 s; perf minus runtime span ranges 0.359619–1.233058 s; enclosing span minus perf ranges 0.190169–0.285359 s. The acquired summary is unchanged with no verification differences. Full-PSD/conditioning failure remains authoritative despite exact proposed in-band PSDs. The saved self-test receipt also records 47 passes.

The diagnostic establishes post-run installation identity and consistency with recorded worker versions. **It cannot retrospectively establish the exact Torch binaries loaded by the workers.** In particular, the post-run `_C` hash is not an acquisition-time worker binary hash. The verifier and updated usage document preserve this limitation. Other previously disclosed limits remain: untransferred source/input bytes rely on acquisition pins, the optional source-review receipt lacks a per-file manifest hash, and conditioned strain equality relies on recorded digests/metadata rather than saved samples.

Final receipt SHA-256: `verification.json` = `0dc09137fdd73c381555d0edae1f3bbcf599f6d2103ae34c4653d6936bc1c3da`; `verifier-self-tests.json` = `da6c84a02821e481f53af7aab6d1ad3822a3166c4a577e13c693f34ab0667750`. Diagnostic SHA-256: `7ca88cfbe69954d16ab8dd5585db26aadc8a77e411225bd7ebbd90ff1a5cb494`; script SHA-256: `7e0ae47390539eb5b86b8e6879ab3a6576133ce68d760d222dd244bd895572f8`. The local acquisition copies match these frozen hashes and the final report's evidence inventory. The original dependency manifest and timing-status hashes also match that inventory and the diagnostic bindings.

This follow-up report is the only persistent output created by this review. No verifier, source, controller, usage document, acquisition evidence or prior report was edited; no remote commands or benchmark reruns occurred.
