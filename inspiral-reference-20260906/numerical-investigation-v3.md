Commit `837f38d493420043e45fb1ad210a0ccf68bacbaa` preserves the CUDA chi-squared phase coefficient at kernel launch. A captured-input replay identifies the remaining error as a **float32 scalar conversion before the kernel's float64 phase arithmetic**. The corrected launch removes all budget violations in those captured cases. **The final source passes its Linux unit suite: 369 passed, 68 skipped and 8 subtests passed. Full CLI parity remains pending; no performance result is claimed.**

The [preserved v2 investigation](/Users/xangma/repos/pycbc/artifacts/torch-inspiral-reference-20260906/numerical-investigation-v2.md) documents the earlier prefix, CPU phase, strain FFT and PSD fixes. On its source `f2c0abe61e787a26f41208f489c62c877bbd5667`, all 475 identities matched across backends; Torch CPU passed every metric, while CUDA still had **302 chi-squared violations**, maximum difference `0.721248627`. SNR, phase and normalization passed. The v2 Linux result of 365 passed, 68 skipped and 8 subtests applies to that parent commit.

For the worst template, `-6051532311927781382`, the new capture records five filter segments and three segments containing selected chi-squared points. The first CPU wrapper failed because it incorrectly required five captured chi-squared segments; it recorded three. The corrected CPU and CUDA wrappers completed, each recording segments 1, 2 and 3. Their source was clean and input hashes remained unchanged. The failed wrapper and both successful captures remain preserved.

In Triton **3.7.1**, the host coefficient `2π/2²¹ = 2.996056226339143e-6` was specialized as `fp32`, yielding `2.996056309712003e-6`. Promoting that value inside the kernel cannot recover its lost bits. Wrapping the launch argument with `tl.constexpr(two_pi_over_N)` preserves the original value. The [coefficient replay](/Users/xangma/repos/pycbc/artifacts/torch-inspiral-reference-20260906/chisq-coefficient-v4.json) changes only that argument wrapper while holding each capture's correlations, SNRs, normalization, bins and points fixed.

| Segment 1, two selected points | Original fused CUDA error | Torch fallback error | Preserved-coefficient fused error | Original / corrected budget violations |
| --- | ---: | ---: | ---: | ---: |
| CPU-captured inputs | 0.721239675 | 2.49210e-6 | 4.04228e-6 | 2 / 0 |
| CUDA-captured inputs | 0.721237407 | 4.03919e-6 | 9.99272e-6 | 2 / 0 |

Errors are maximum absolute differences from independent direct complex128 phases. Recorded launches confirm both corrected two-point cases used the fused Triton kernel. All **eight selected samples across six capture rows** pass the existing budget with the corrected coefficient. Segments 2 and 3 each have one selected point and use the existing fallback; they do not test the fused kernel. Across all six rows, the largest corrected error is `1.39455e-5`.

The first diagnostic attempt stopped because `diagnose-chisq-inputs.py` was missing; [its failed log](/Users/xangma/repos/pycbc/artifacts/torch-inspiral-reference-20260906/chisq-coefficient-v4.log) is retained alongside the [successful retry log](/Users/xangma/repos/pycbc/artifacts/torch-inspiral-reference-20260906/chisq-coefficient-v4b.log). The successful record binds the replay script, helper, source and captured arrays with identical hashes before and after execution.

Relative to its parent, the new commit changes only the Python launch call in `pycbc/vetoes/chisq_torch.py` and extends `test/test_chisq_precision.py`. **The Triton kernel definition and body are unchanged**, verified by comparing their parsed syntax trees; native module hashes also match. The regression covers CPU/CUDA, both correlation precisions and NumPy/PyCBC indices, using direct complex128 phases over `2²¹` samples and late points. Complex64 CUDA cases require a recorded fused-kernel launch and CUDA output residency. Existing test tolerances and all scientific budgets remain unchanged.

The [final source unit receipt](/Users/xangma/repos/pycbc/artifacts/torch-inspiral-reference-20260906/unit-tests-v5.json) and its hashed log record all 16 selected modules completing on host len with return code zero: 369 passed, 68 skipped, 8 subtests passed and 21 warnings, in 24.89 seconds of pytest time (27.02 seconds including the runner). Source 837f38d was clean before and after; every recorded input hash matched and was verified against local files or the committed source. This suite includes the extended regression. These durations describe validation only.

The [companion JSON](/Users/xangma/repos/pycbc/artifacts/torch-inspiral-reference-20260906/numerical-investigation-v3.json) binds all eight corrected source/test blobs, source manifests, captured arrays, failed attempts, successful replay and the complete v2 evidence chain. These two new report files are the only changes made for this synthesis.

This replay establishes the cause on one template with fixed inputs. The final source still needs full CLI comparisons, repeated CUDA qualification, compression/boundary checks and matched timings. All scientific budgets remain `atol=1e-5`, `rtol=1e-4`, phase `1e-4` radians and normalization `rtol=1e-5`; the 302-violation full CLI failure is not cleared by replay alone.
